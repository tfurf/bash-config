#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
# List the things you want sourced here. A decent way to turn on/off functions that are stored here.
source $DIR/dir.bash
source $DIR/ros-tools.bash
source $DIR/wm-tools.bash
